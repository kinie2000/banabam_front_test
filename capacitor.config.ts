import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'Test_Banabam_ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
